default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000

production:
  adapter: postgresql
  url: <%= ENV["DATABASE_URL"] %>

  cable:
    <<: *default
    database: matrix_dut9

  queue:
    <<: *default
    database: matrix_dut9